version: '3'

name: 'slack-translate'

services:
  redis:
    container_name: slack-translate-cache
    image: redis:latest
    command: --port 6380
    volumes:
      - redis:/data
    ports:
      - '127.0.0.1:6380:6380'

volumes:
  redis:
